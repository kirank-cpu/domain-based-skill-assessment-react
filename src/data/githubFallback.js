// Offline fallback bank for the dynamic "GitHub & CI/CD" domain.
// Used only when the Claude API is unavailable (rate-limited, no key, network
// error). Each level holds 20 distinct questions so the app can still serve a
// strict 20-question assessment fully offline.
//
// Shape: { id, type: 'mcq'|'scenario', question, options[4], answer(index), explanation }

export const GITHUB_FALLBACK = {
  beginner: [
    { id: 'gh-b-1', type: 'mcq', question: 'What is the primary purpose of Git?', options: ['Compiling source code', 'Distributed version control for tracking code changes', 'Deploying applications to servers', 'Managing cloud infrastructure'], answer: 1, explanation: 'Git is a distributed version control system that tracks changes to files and coordinates work among multiple people.' },
    { id: 'gh-b-2', type: 'mcq', question: 'Which command stages all modified files for the next commit?', options: ['git commit -a', 'git add .', 'git push', 'git stage --all'], answer: 1, explanation: '`git add .` stages all changes in the current directory tree so they are included in the next commit.' },
    { id: 'gh-b-3', type: 'mcq', question: 'What does CI stand for in CI/CD?', options: ['Continuous Integration', 'Code Inspection', 'Container Instance', 'Central Index'], answer: 0, explanation: 'CI stands for Continuous Integration — automatically building and testing code each time changes are pushed.' },
    { id: 'gh-b-4', type: 'mcq', question: 'Where are GitHub Actions workflow files stored in a repository?', options: ['/actions', '/.github/workflows', '/ci', '/workflows.yml'], answer: 1, explanation: 'GitHub Actions reads workflow YAML files from the `.github/workflows/` directory.' },
    { id: 'gh-b-5', type: 'mcq', question: 'What is a "commit" in Git?', options: ['A branch of the repository', 'A snapshot of changes saved to the repository history', 'A remote server', 'A merge conflict'], answer: 1, explanation: 'A commit records a snapshot of the staged changes along with a message, becoming part of the project history.' },
    { id: 'gh-b-6', type: 'mcq', question: 'What does `git clone` do?', options: ['Creates a new empty repository', 'Copies a remote repository to your local machine', 'Deletes a branch', 'Merges two branches'], answer: 1, explanation: '`git clone` downloads a full copy of a remote repository, including its history, to your local machine.' },
    { id: 'gh-b-7', type: 'mcq', question: 'What is a Pull Request (PR) on GitHub?', options: ['A request to delete a repository', 'A proposal to merge changes from one branch into another, with review', 'A command to pull remote changes', 'A backup of the repository'], answer: 1, explanation: 'A Pull Request proposes merging changes from one branch into another and enables code review and discussion.' },
    { id: 'gh-b-8', type: 'mcq', question: 'Which file typically lists files Git should ignore?', options: ['.gitkeep', '.gitignore', '.ignore', 'ignore.txt'], answer: 1, explanation: 'The `.gitignore` file specifies intentionally untracked files that Git should ignore (e.g. build output, secrets).' },
    { id: 'gh-b-9', type: 'mcq', question: 'What does `git push` do?', options: ['Downloads changes from the remote', 'Uploads local commits to a remote repository', 'Stages files', 'Reverts the last commit'], answer: 1, explanation: '`git push` uploads your local commits to the corresponding branch on the remote repository.' },
    { id: 'gh-b-10', type: 'mcq', question: 'In a GitHub Actions workflow, what triggers the workflow to run?', options: ['The `jobs` key', 'The `on` key (events like push, pull_request)', 'The `steps` key', 'The `env` key'], answer: 1, explanation: 'The `on` key defines the events (such as `push` or `pull_request`) that trigger the workflow.' },
    { id: 'gh-b-11', type: 'mcq', question: 'What is a branch in Git?', options: ['A copy of the remote server', 'An independent line of development', 'A type of commit message', 'A merge conflict marker'], answer: 1, explanation: 'A branch is a movable pointer to a series of commits, allowing independent lines of development.' },
    { id: 'gh-b-12', type: 'mcq', question: 'What does CD commonly stand for in CI/CD?', options: ['Code Delivery', 'Continuous Deployment (or Delivery)', 'Central Deployment', 'Container Delivery'], answer: 1, explanation: 'CD stands for Continuous Deployment or Continuous Delivery — automating the release of validated changes.' },
    { id: 'gh-b-13', type: 'mcq', question: 'Which command shows the current state of the working directory and staging area?', options: ['git log', 'git status', 'git diff', 'git show'], answer: 1, explanation: '`git status` displays which files are staged, unstaged, or untracked.' },
    { id: 'gh-b-14', type: 'mcq', question: 'What is the default branch name for new GitHub repositories today?', options: ['master', 'trunk', 'main', 'default'], answer: 2, explanation: 'GitHub now uses `main` as the default branch name for new repositories.' },
    { id: 'gh-b-15', type: 'scenario', question: 'You made changes but want to discard everything and match the last commit. Which is safest to reason about first?', options: ['git push --force', 'Review with `git status`/`git diff`, then `git restore` the files', 'git clone again', 'Delete the repo'], answer: 1, explanation: 'Inspect changes first, then use `git restore` (or `git checkout -- <file>`) to discard uncommitted changes safely.' },
    { id: 'gh-b-16', type: 'mcq', question: 'What is a "runner" in GitHub Actions?', options: ['A YAML linter', 'A server that executes your workflow jobs', 'A type of trigger', 'A Git command'], answer: 1, explanation: 'A runner is a machine (GitHub-hosted or self-hosted) that executes the jobs defined in a workflow.' },
    { id: 'gh-b-17', type: 'mcq', question: 'What does `git pull` effectively do?', options: ['Only fetches without merging', 'Fetches remote changes and merges them into the current branch', 'Pushes local commits', 'Creates a new branch'], answer: 1, explanation: '`git pull` is `git fetch` followed by `git merge` (or rebase), updating your branch with remote changes.' },
    { id: 'gh-b-18', type: 'mcq', question: 'What is the smallest unit of work in a GitHub Actions workflow?', options: ['A job', 'A step', 'A stage', 'A pipeline'], answer: 1, explanation: 'Steps are the individual tasks within a job; each step runs a command or an action.' },
    { id: 'gh-b-19', type: 'mcq', question: 'Why should secrets (API keys, tokens) NOT be committed to a repository?', options: ['They slow down cloning', 'They become visible in history and can be leaked/exploited', 'Git cannot store them', 'They break YAML parsing'], answer: 1, explanation: 'Committed secrets remain in Git history and may be exposed publicly; use encrypted secrets/vaults instead.' },
    { id: 'gh-b-20', type: 'scenario', question: 'A teammate pushed commits to `main`. Your local `main` is behind. What is the typical first step before pushing your work?', options: ['Force push over their changes', 'Pull/fetch and integrate their changes first', 'Delete your local branch', 'Open a new repository'], answer: 1, explanation: 'Integrate remote changes (pull or fetch + merge/rebase) before pushing to avoid rejecting or clobbering their work.' },
  ],
  intermediate: [
    { id: 'gh-i-1', type: 'mcq', question: 'What is the difference between `git merge` and `git rebase`?', options: ['They are identical', 'Merge creates a merge commit preserving history; rebase rewrites commits onto a new base for a linear history', 'Rebase is only for remotes', 'Merge deletes branches'], answer: 1, explanation: 'Merge preserves the branching history with a merge commit; rebase replays commits onto another base, producing a linear history.' },
    { id: 'gh-i-2', type: 'mcq', question: 'In GitHub Actions, how do you run jobs only after another job succeeds?', options: ['Use `depends-on`', 'Use the `needs` keyword', 'Use `after`', 'Jobs always run sequentially'], answer: 1, explanation: 'The `needs` keyword declares job dependencies so a job waits for the referenced job(s) to complete successfully.' },
    { id: 'gh-i-3', type: 'mcq', question: 'What is a matrix build in GitHub Actions used for?', options: ['Encrypting secrets', 'Running the same job across multiple configurations (e.g. OS/versions) in parallel', 'Merging branches', 'Caching dependencies'], answer: 1, explanation: 'A `strategy.matrix` runs a job across combinations of variables (OS, language versions) in parallel.' },
    { id: 'gh-i-4', type: 'scenario', question: 'Your CI pipeline reinstalls all dependencies on every run, making it slow. What is the best improvement?', options: ['Remove the tests', 'Cache dependencies keyed on the lockfile hash (e.g. actions/cache)', 'Run only on weekends', 'Use a bigger runner only'], answer: 1, explanation: 'Caching dependencies keyed on the lockfile hash avoids redundant downloads and dramatically speeds up CI.' },
    { id: 'gh-i-5', type: 'mcq', question: 'What does branch protection with "require status checks to pass" enforce?', options: ['Faster cloning', 'Merges are blocked until required CI checks succeed', 'Automatic deployments', 'Secret rotation'], answer: 1, explanation: 'Branch protection can require CI status checks to pass before a PR can be merged, safeguarding the protected branch.' },
    { id: 'gh-i-6', type: 'mcq', question: 'What is `git cherry-pick` used for?', options: ['Deleting commits', 'Applying a specific commit from one branch onto another', 'Squashing all commits', 'Rebasing interactively'], answer: 1, explanation: '`git cherry-pick <sha>` applies the changes from a specific commit onto the current branch.' },
    { id: 'gh-i-7', type: 'mcq', question: 'How are encrypted secrets referenced inside a GitHub Actions workflow?', options: ['${{ env.SECRET }}', '${{ secrets.MY_SECRET }}', '$SECRET', '{{ secret:MY_SECRET }}'], answer: 1, explanation: 'Secrets are accessed via the `secrets` context, e.g. `${{ secrets.MY_SECRET }}`, and are masked in logs.' },
    { id: 'gh-i-8', type: 'scenario', question: 'A feature branch has many messy WIP commits. Before merging, you want a clean, meaningful history. What is a good approach?', options: ['Force push to main', 'Interactive rebase (squash/fixup) or squash-merge the PR', 'Delete the branch and start over', 'Add more commits'], answer: 1, explanation: 'An interactive rebase to squash/reword, or a squash-merge on the PR, condenses noisy commits into clean, meaningful ones.' },
    { id: 'gh-i-9', type: 'mcq', question: 'What is the purpose of `actions/checkout` in a workflow?', options: ['To cache node modules', 'To check out your repository code onto the runner', 'To deploy the app', 'To create a release'], answer: 1, explanation: 'The `actions/checkout` action clones your repository onto the runner so subsequent steps can access the code.' },
    { id: 'gh-i-10', type: 'mcq', question: 'What causes a Git merge conflict?', options: ['Two branches modifying the same lines differently', 'Pushing to a remote', 'Cloning a large repo', 'Running CI'], answer: 0, explanation: 'A merge conflict occurs when two branches change the same lines (or one deletes what another edits) and Git cannot auto-merge.' },
    { id: 'gh-i-11', type: 'mcq', question: 'What is the difference between GitHub Actions "environment" secrets and repository secrets?', options: ['No difference', 'Environment secrets are scoped to a deployment environment and can require approvals; repo secrets apply repository-wide', 'Environment secrets are unencrypted', 'Repo secrets cannot be used in workflows'], answer: 1, explanation: 'Environment secrets are scoped per deployment environment and can add protection rules/approvals; repository secrets are available across the repo.' },
    { id: 'gh-i-12', type: 'scenario', question: 'You need a deploy job to run only when pushing to `main`, not on pull requests. How?', options: ['Use `if: github.ref == \'refs/heads/main\'` and appropriate `on` triggers', 'Delete the PR trigger from all workflows', 'Use a self-hosted runner', 'Run deploy manually every time'], answer: 0, explanation: 'Combine event filters with a conditional like `if: github.ref == \'refs/heads/main\'` (and `on: push` branches) to gate deploys.' },
    { id: 'gh-i-13', type: 'mcq', question: 'What does `git stash` do?', options: ['Permanently deletes changes', 'Temporarily shelves uncommitted changes so you can switch context', 'Pushes to remote', 'Creates a tag'], answer: 1, explanation: '`git stash` saves uncommitted changes aside so you can work on something else, then `git stash pop` to restore them.' },
    { id: 'gh-i-14', type: 'mcq', question: 'What is a GitHub Actions "composite action"?', options: ['A Docker image', 'A reusable action that bundles multiple steps into one', 'A secret store', 'A matrix strategy'], answer: 1, explanation: 'A composite action packages multiple workflow steps into a single reusable action.' },
    { id: 'gh-i-15', type: 'mcq', question: 'What is the recommended way to authenticate a workflow to a cloud provider without long-lived secrets?', options: ['Hard-code credentials', 'OpenID Connect (OIDC) federation for short-lived tokens', 'Store the password in the README', 'Disable authentication'], answer: 1, explanation: 'OIDC lets workflows exchange a short-lived identity token with the cloud provider, avoiding stored long-lived credentials.' },
    { id: 'gh-i-16', type: 'scenario', question: 'CI passes locally but fails in the pipeline with "works on my machine" symptoms. What is the most likely root cause to investigate?', options: ['The runner is broken', 'Environment differences — versions, env vars, or uncommitted local files', 'GitHub is down', 'The YAML is invalid'], answer: 1, explanation: 'Such failures usually stem from environment drift: differing tool versions, missing env vars, or files not committed to the repo.' },
    { id: 'gh-i-17', type: 'mcq', question: 'What does the `GITHUB_TOKEN` provide in a workflow?', options: ['Your personal password', 'An automatically-created, scoped token to interact with the repo/API for that run', 'Access to all repositories', 'A permanent deploy key'], answer: 1, explanation: 'GitHub automatically creates a scoped `GITHUB_TOKEN` per run for authenticating API/repo operations, expiring when the job ends.' },
    { id: 'gh-i-18', type: 'mcq', question: 'What is the benefit of running tests in parallel jobs in CI?', options: ['Higher cost only', 'Reduced total feedback time by splitting the workload', 'Fewer tests run', 'It disables caching'], answer: 1, explanation: 'Parallelizing/sharding tests across jobs reduces overall pipeline duration and speeds up feedback.' },
    { id: 'gh-i-19', type: 'mcq', question: 'What is `git revert` (vs `git reset`)?', options: ['Both rewrite history identically', 'Revert creates a new commit undoing changes (safe on shared branches); reset moves the branch pointer (can rewrite history)', 'Revert deletes the repo', 'Reset uploads to remote'], answer: 1, explanation: '`git revert` records a new commit that undoes a previous one — safe for shared history — whereas `git reset` moves the branch pointer and can rewrite history.' },
    { id: 'gh-i-20', type: 'scenario', question: 'You want to reuse the same CI logic across many repositories. What GitHub feature helps most?', options: ['Copy-paste the YAML everywhere', 'Reusable workflows (workflow_call) or a shared composite action', 'One giant monorepo only', 'Manual runs'], answer: 1, explanation: 'Reusable workflows (`on: workflow_call`) and shared/composite actions let multiple repos consume centralized CI logic.' },
  ],
  complex: [
    { id: 'gh-c-1', type: 'scenario', question: 'Your deployment pipeline must ship to production with near-zero downtime and instant rollback. Which strategy fits best?', options: ['Deploy directly over the running servers', 'Blue-green or canary deployment with automated health checks and rollback', 'Manual FTP upload', 'Restart everything at once'], answer: 1, explanation: 'Blue-green/canary deployments route traffic to a new version gradually with health checks, enabling instant rollback and minimal downtime.' },
    { id: 'gh-c-2', type: 'mcq', question: 'In a trunk-based development model, how are long-lived feature branches typically handled?', options: ['Encouraged for months', 'Avoided in favor of short-lived branches merged frequently behind feature flags', 'Never merged', 'Replaced by forks only'], answer: 1, explanation: 'Trunk-based development favors short-lived branches merged frequently, using feature flags to hide incomplete work in trunk.' },
    { id: 'gh-c-3', type: 'scenario', question: 'A CI workflow using `pull_request_target` runs with repo secrets. Why is this a security risk with untrusted forks?', options: ['It is slower', 'It executes in the base repo context with secret access, so malicious PR code could exfiltrate secrets', 'It cannot access the code', 'It disables caching'], answer: 1, explanation: '`pull_request_target` runs in the base repository context with access to secrets; running untrusted fork code there can leak secrets — avoid checking out/executing untrusted code with it.' },
    { id: 'gh-c-4', type: 'mcq', question: 'What is the primary goal of pinning third-party GitHub Actions to a full commit SHA instead of a tag?', options: ['Faster runs', 'Supply-chain security — a tag can be moved to malicious code, a SHA is immutable', 'Smaller logs', 'Automatic updates'], answer: 1, explanation: 'Tags are mutable and can be repointed to malicious commits; pinning to a full commit SHA guarantees you run the exact reviewed code.' },
    { id: 'gh-c-5', type: 'scenario', question: 'Two teams frequently hit merge conflicts in a shared monorepo CI. Which combination best reduces friction and keeps main releasable?', options: ['Stop testing', 'Merge queues + required checks + smaller, frequent PRs + code owners', 'One weekly big-bang merge', 'Force pushes to main'], answer: 1, explanation: 'Merge queues serialize and re-validate merges, while small frequent PRs, required checks, and CODEOWNERS reduce conflicts and keep main releasable.' },
    { id: 'gh-c-6', type: 'mcq', question: 'What does a "merge queue" (GitHub) solve?', options: ['It caches dependencies', 'It ensures PRs are tested against the latest main before merging, preventing broken main from semantic conflicts', 'It signs commits', 'It rotates secrets'], answer: 1, explanation: 'A merge queue re-tests each PR against the up-to-date base (and batches) so logically-conflicting-but-textually-clean merges do not break main.' },
    { id: 'gh-c-7', type: 'scenario', question: 'You must run CI for untrusted fork PRs but also need integration secrets for trusted runs. What is the safest design?', options: ['Give all forks the secrets', 'Split workflows: run untrusted code with `pull_request` (no secrets), and gate secret-using jobs behind manual approval/label or `environment` protection', 'Disable CI for forks', 'Use pull_request_target with checkout of PR head'], answer: 1, explanation: 'Run untrusted code without secrets on `pull_request`; require maintainer approval (label/environment protection) before any secret-bearing job runs.' },
    { id: 'gh-c-8', type: 'mcq', question: 'What is the purpose of generating and attaching an SBOM and provenance (SLSA) in a release pipeline?', options: ['To speed up builds', 'To provide verifiable supply-chain integrity and traceability of artifacts', 'To reduce artifact size', 'To skip tests'], answer: 1, explanation: 'An SBOM plus signed provenance (e.g. SLSA attestations) proves what went into an artifact and that it was built by a trusted pipeline.' },
    { id: 'gh-c-9', type: 'scenario', question: 'A pipeline intermittently fails at the deploy step ~5% of the time due to a downstream API timeout. What is the most robust fix?', options: ['Retry the whole pipeline manually', 'Add bounded retries with exponential backoff and idempotent deploys, plus health verification', 'Ignore the failures', 'Increase the timeout to infinity'], answer: 1, explanation: 'Bounded retries with backoff on idempotent operations, combined with post-deploy health checks, handle transient downstream failures robustly.' },
    { id: 'gh-c-10', type: 'mcq', question: 'What does GitOps mean in a CD context?', options: ['Manually SSHing to servers', 'Using Git as the single source of truth for declarative infrastructure/app state, with automated reconciliation', 'Storing secrets in Git', 'Running Git on production'], answer: 1, explanation: 'GitOps treats Git as the source of truth for declarative desired state; a controller (e.g. Argo CD/Flux) continuously reconciles the cluster to match it.' },
    { id: 'gh-c-11', type: 'scenario', question: 'Your workflow needs least-privilege access to only create a release and read contents. How do you configure it?', options: ['Grant `permissions: write-all`', 'Set explicit `permissions:` (e.g. contents: read, releases: write) at workflow/job level', 'Use a personal admin token', 'Disable permissions'], answer: 1, explanation: 'Set the `permissions:` block to the minimal scopes the job needs, following least privilege rather than the default broad token.' },
    { id: 'gh-c-12', type: 'mcq', question: 'Why prefer OIDC over storing static cloud credentials as GitHub secrets for deployments?', options: ['OIDC is easier to type', 'OIDC issues short-lived, workload-scoped tokens, eliminating long-lived secrets that can leak', 'OIDC is free only', 'Static secrets are unsupported'], answer: 1, explanation: 'OIDC federation exchanges a short-lived, scoped token per run, removing standing credentials that could be exfiltrated or misused.' },
    { id: 'gh-c-13', type: 'scenario', question: 'A monorepo build recompiles everything on any change, wasting compute. What technique targets only affected projects?', options: ['Always full rebuild', 'Path filters plus an affected-graph build tool (e.g. Nx/Bazel/Turborepo) with remote caching', 'Delete unused code', 'Bigger runners only'], answer: 1, explanation: 'Path filters and dependency-graph-aware build tools with remote caching build/test only the projects affected by a change.' },
    { id: 'gh-c-14', type: 'mcq', question: 'What is the purpose of requiring signed commits (GPG/Sigstore) on a protected branch?', options: ['Faster merges', 'Verifying author identity and integrity to prevent commit spoofing', 'Smaller repo size', 'Automatic deployment'], answer: 1, explanation: 'Signed commits cryptographically verify authorship and integrity, preventing spoofed or tampered commits on protected branches.' },
    { id: 'gh-c-15', type: 'scenario', question: 'You need canary deploys that auto-rollback when error rates spike. What must the pipeline integrate?', options: ['Only unit tests', 'Progressive traffic shifting tied to real-time metrics/SLOs with automated rollback (e.g. Argo Rollouts/Flagger)', 'Manual monitoring only', 'A single big deploy'], answer: 1, explanation: 'Automated canary analysis shifts traffic gradually while watching metrics/SLOs and rolls back automatically when thresholds are breached.' },
    { id: 'gh-c-16', type: 'mcq', question: 'What problem do ephemeral, per-PR "preview environments" solve?', options: ['They reduce test coverage', 'They let reviewers validate changes in an isolated live environment before merge', 'They store secrets', 'They replace CI'], answer: 1, explanation: 'Per-PR preview environments deploy the branch to an isolated environment so changes can be reviewed live before merging.' },
    { id: 'gh-c-17', type: 'scenario', question: 'A self-hosted runner is used for public-repo PRs. Why is this dangerous, and what mitigates it?', options: ['No danger', 'Untrusted PR code can run on your infrastructure and persist between jobs; use ephemeral/isolated runners and restrict to trusted events', 'It only slows builds', 'Use a bigger disk'], answer: 1, explanation: 'Persistent self-hosted runners can be compromised by untrusted PR code; use ephemeral, isolated runners and avoid running untrusted code on them.' },
    { id: 'gh-c-18', type: 'mcq', question: 'What is a key advantage of declarative pipeline definitions stored in the repo (pipeline-as-code)?', options: ['They cannot be reviewed', 'Versioned, reviewable, reproducible pipelines that evolve with the code', 'They run faster than everything', 'They remove the need for tests'], answer: 1, explanation: 'Pipeline-as-code makes CI/CD versioned, peer-reviewed, and reproducible alongside the application code.' },
    { id: 'gh-c-19', type: 'scenario', question: 'Release notes and versioning are inconsistent across releases. Which approach automates this reliably?', options: ['Write them manually each time', 'Conventional Commits + automated semantic-release/changelog generation in CI', 'Skip release notes', 'Tag randomly'], answer: 1, explanation: 'Adopting Conventional Commits with automated semantic versioning/changelog tools generates consistent versions and release notes in CI.' },
    { id: 'gh-c-20', type: 'mcq', question: 'What does "shift-left security" mean in a CI/CD pipeline?', options: ['Running security scans only in production', 'Integrating security checks (SAST, dependency/secret scanning) early in the pipeline', 'Disabling security to move faster', 'Manual audits once a year'], answer: 1, explanation: 'Shift-left security embeds automated checks (SAST, dependency and secret scanning) early in CI so issues are caught before release.' },
  ],
}

// Returns up to `count` distinct fallback questions for a level, shuffled for variety.
export function getFallbackQuestions(levelId, count = 20) {
  const pool = GITHUB_FALLBACK[levelId] ?? []
  const shuffled = [...pool]
  for (let i = shuffled.length - 1; i > 0; i--) {
    const j = Math.floor(Math.random() * (i + 1))
    ;[shuffled[i], shuffled[j]] = [shuffled[j], shuffled[i]]
  }
  return shuffled.slice(0, count)
}
