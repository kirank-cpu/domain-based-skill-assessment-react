version: 0.2

phases:
  install:
    runtime-versions:
      nodejs: 20
    commands:
      - npm ci
  build:
    commands:
      - npm test
      - npm run build

artifacts:
  files:
    - '**/*'
  discard-paths: no
  base-directory: .

cache:
  paths:
    - '/root/.npm/**/*'
